# HANDOFF · ai.html, JAde Wii, AI Solutions Engineer portfolio

Serve: `python3 /tmp/nocache_server.py` (no-cache, port 8765) or `cd ~/Downloads/jadewii-combined && python3 -m http.server 8765`, then open http://localhost:8765/ai.html
QA: headless Chrome or the CDP scripts in the scratchpad (`cdp2.mjs <url> <secs> [shot]`, `cdp-eval.mjs <url> <secs> <expr>`). virtual-time screenshots freeze the rAF loop; use the CDP scripts to test the cycle/animation/live iframes in real time.
index.html is the OTHER window's live designer site, NEVER touch or commit it (nor assets/site.js, artwork.html).

## What the page is
A hero, then the PROCESS RACK: 14 real projects, each a row of modules (a real source line with its file:line) wired with Eurorack Grid cables, over a STAGE (a terminal window: real commands + output on the left, the project itself on the right). It auto-plays: types the problem, walks the signal, moves to the next, loops. A numbered BANK jumps between projects. 23 themes (button top-right, Shift+T). Below the rack: AI Work, AI & Human Oversight, Technology Background, Plugins/Web Apps/Websites/iOS Apps, and a button that reveals the Educational-videos/music (YouTube) sections.

## Laws (keep these)
1. **AI SOLUTIONS ENGINEER is the point.** Every choice sharpens that title. YouTube and tangential creative work sit behind the bottom button, not on the main flow. Don't re-add breadth (the 6 web-instrument racks were removed as scope creep).
2. **No made-up content.** Every module quotes a real line with its file:line. Every terminal line is a command actually run with its real output. Moving repos (~/hybrid-jammin, ~/modular-lab) get pinned to a commit (`git show <sha>:path`).
3. **Every visualization must MEAN something.** The module viz is chosen from its content: numbers→bars (heights = the real numbers), lists→one LED per item, audio→its waveform, timing→an envelope, sequence→step cells, mapping→a connector, real parameters (CC/knob/filter/rate)→knobs. Plain code with no natural diagram shows NO viz, just the clean code line. Never decorative/random controls.
4. **Knobs are the STERN set, ported 1:1 from ~/modular-lab/case/toi.js** (metal, fluted, dark fluted, pointer, numeral dial, ticks). Each project uses a different style. Slots 2/8/14 use the uncolored STERN knob in a lighter theme shade (`k_sternLight`, `STERN_LIGHT` set in patch.js).
5. **Use what we built, verbatim**, real assets, not lookalikes (STERN knobs, Eurorack Grid cables, real deck data, the pixel avatar engine).
6. **Show the real product, curated, never a live site in the box.** Jade, 2026-09-07: "i dont wanna see embed sites in the box area, each section should look like the AI use-case screener and the plant section, not a website but words explaining and ONE element from that site". Every stage is a demo-<slug>.html: title, one sub line, THE ELEMENT (ported verbatim from the real source with a file:line comment), a lede, a facts row (real numbers with the command that produced them), one or two short paragraphs about the project. Template = demo-hardware.html / demo-agents.html. The brief the builder agents followed is in the scratchpad as STAGE-BRIEF.md (copy it into this folder if a fresh window needs it).
7. Fixed slots, no layout shift, crossfade off (hard cut), inner scroll, adapts to all 23 themes. Only the hero role line is bold. NO em dashes anywhere (copy law). No tacked-on 'why this is the job' self-explaining lines, let the work speak.

## Current state (2026-09-07)
- 17 projects: UNSKOOLERZ, HOW I RUN AGENTS, PATCH ASSISTANT, SAFETENSORS-CHECK, AI USE-CASE SCREENER, STUDIO PLANTS, JAMNUTZ HARDWARE, **MANGLER (new, slot 8)**, TOI SEMIS, JAMNUTZ DESKTOP, JELLYBOI, NOISEFACE, WAVEFACE, TODOMAI, JAMNUTZ · KEYBOARD, EURORACK HARDWARE, EURORACK GRID.
- MANGLER: Jade asked for "the jamnutz sound mangler... add a play button and load some of our music that people can play and mangle live, then explain how this is [the job] related without saying the words". demo-mangler.html = manglerChain.js ported verbatim (~/Documents/jamworld/src/lib/audio/manglerChain.js), the FxUnit knob tables verbatim, OutlineKnob/OutlineFader from PublicBeat.jsx, the line scope, three tracks in music/ (fearmonger.mp3 from ~/jadewii.com/public/audio, bubblegum-jungle.mp3 and the-middle-of-a-dream.mp3 from the ~/Downloads Suno album pulls, ~10MB total). The last paragraph is the explanation she asked for. Rack = 5 real lines from manglerChain.js + mangler-ios/PLAN.md; terminal = real wc/grep/git/sed output.
- Mobile QA at 390 and 820, all 17 projects: rack scrolls sideways (220px modules), stage stacks, 17-slot bank fits. Fixed: the stage head overflowed on long names under 600px (st-name now one line with ellipsis, arrows 36px, patch.css); the Sound Design 23-column circle grid forced the page to 1151px between 701 and 1180px (ai.html, `minmax(0,1fr)` + circles sized by column, desktop unchanged).
- Law 3 sweep, LIGHT and DARK, every module: 13 modules had a viz that meant nothing (an envelope on the Anthropic call, sine waves on gate/skip-list code, knobs on a prompt sentence and on drawCable...). patch.js now takes an optional 7th field on a module, an explicit viz (`'none'`, `'flow'`, `'wave'`, `'env'`, `'leds:10'`), used on those 13; `vizFor` keywords untouched otherwise.
- Stages converted to the curated style (Law 6): UNSKOOLERZ (the real TradingCard frame + LION card, tutor rule, nightly factory), JELLYBOI (watch case + real capture + PROJECT.md), NOISEFACE (12 real colors, real per-sample generators, audible), WAVEFACE (real seam/loop buffer, drawn + playable), TODOMAI (real mode system + voice router). JAMNUTZ · KEYBOARD got its "where it comes from" paragraph. Also done: JAMNUTZ HARDWARE (the real MIDI map, tap channels/CCs/drum notes), JAMNUTZ DESKTOP (the real 223-entry plugin scan + ccToKnob), TOI SEMIS (the JW-CZ panel, 26 params on the real toimetal knob, 15 jacks), EURORACK GRID (the 101-code roster in 14 brand colors). Every `load:{` in patch.js now points at a demo-*.html; no live iframes, no screenshots.
- **No window at all (Jade, later the same day: "i dont want it inside a window, i want it part of the page like the modules").** The stage box is gone. `#patchFeature` (class .thework, ai.html) is an inline section: a head row (OUT · THE WORK, name, result chip, OPEN), then `.ft-mount` where the project's demo-*.html is fetched and mounted INTO the page (patch.js mountDemo: the demo's <style> nested under .ft-mount, its inline scripts run in a function scope, rAF/timers/AudioContext/Audio tagged to the mount and torn down on the next project). Template pages split into `.ft-el` (the element, in a module-style panel, left) and `.ft-words` (right); the four older doc pages (patch assistant, safetensors, screener, plants) mount whole as `.ft-doc`. The terminal is a plain panel under both, no dots, no chrome. Height is content height (the section below moves on a project change; if Jade wants it fixed, give .ft-mount a height and inner scroll).
- Contact row in the hero (email + github.com/jadewii). LinkedIn and a résumé link are holds (no résumé on disk outside the Trash).
- unskoolerz/ (the 9.9MB local build) is no longer referenced by any stage; safe to delete when Jade says so.
- CDP scripts: scratchpad cdp-m.mjs (`<url> <w> <h> <secs> [shot] [expr]`, env PORT, SCROLLY or SCROLLSEL, mobile emulation), cdp-strips.mjs (rack strips per project, env THEMES=light,dark ONLY=0,7), measure.js (the 17-project clipping audit). Headless mobile emulation needs a `<meta name="viewport">` on every demo page or scrollWidth reads 980; every demo page has one now.

## Done 2026-09-07 (this window)
- **Reading pause on phones** (patch.js `reading()`): an IntersectionObserver on `#patchFeature` plus the last scroll/touch time; `paused()` also holds while THE WORK is on screen and the reader scrolled or touched under 8 s ago. Verified over CDP at 390: bank progress held at 0.773 for 3 s after a scroll, reached 1.000 after the 8 s window.
- **The four doc pages are now element + words** (each ~80 to 150 lines, the 1,200 lines of copied site CSS are gone, every "why this is on my portfolio" section cut):
  - demo-safetensors-check.html: check.py:22-52 ported to JS, run on the four real fixture files (bytes embedded from xxd), a byte-map of length/header/tensors against the real file, the parse steps, the verdict.
  - demo-patch-assistant.html: validate.py:12-81 ported line for line, the six catalog archetypes the tests use, the ten cases from test_validate.py:12-25 drawn as a small rack (bad cables in the accent), the real error strings.
  - demo-ai-use-case-screener.html: the tier rubric from screen.py:62-72 as a four-column board with the six cases on their expected tier, tap a case to read its text (cases/*.md verbatim). runs/ is empty on this machine (no anthropic module, no key), so there is no saved memo; the facts row says so.
  - demo-studio-plants.html: the shelf kept (DancingPlant.css:4-11 verbatim, the localPulse beat from audioPulse.js:140-150), a --beat meter, words cut to a lede + two paragraphs. Count corrected to 135 (`ls public/plants | grep -c plant_`; the 136th entry is the grow/ folder).
- **Terminal caption overlap fixed** (patch.css .ft-term .lines): long terminals scrolled under the "TERMINAL · every line was run" caption on slots 01/03/05; the lines box now starts under the caption and clips at the top.
- **Dark pass over all 17** (cdp-ft.mjs in the scratchpad, `<url> <w> <h> <project> <theme> <shot>`, env EXTRA=js): no hard-coded theme colors left in the demo pages; the remaining literals are real product colors (brand swatches in the roster, PCB copper, the watch case, the TODOMAI palette, NoiseFace's 12 colors).
- **TOOLS cards under AI Work** now jump to their bank slot (`data-jump` in ai.html, handler in patch.js next to the prev/next buttons) instead of opening the demo page. Card copy corrected to 135 plants and 104 lines.
- **ONE SCREEN, NO SCROLL (Jade, 2026-09-07 afternoon, from her screenshot):** .ft-mount is a 2-column grid, rows 460px + 160px: the element box top-left (fixed 460px, an element taller than that is zoomed down in patch.js fitElement, per-element trims in patch.css for the agents grid and the TODOMAI buttons), the terminal directly under it, the words on the right at 636px showing title, sub, lede, facts and ONE paragraph (patch.js cutWords marks the rest .ft-cut in the mount only; the standalone page keeps everything). Measured over CDP: 0px overflow on all 17 at 1400. Under 1100px it stacks element, terminal, words with no height caps. Words were trimmed on UNSKOOLERZ, PATCH ASSISTANT, SAFETENSORS, SCREENER, PLANTS, JAMNUTZ HARDWARE, MANGLER.
- **Filled left column + arrows (Jade, 2026-09-07, third screenshot: "if the element is not taking up all the space, make the terminal taller, we want this area very filled and active; we need the left and right arrows"):** fitElement sizes the element box to its content (200 to 460px) and gives the rest of the 636px to the terminal (min 160); a taller terminal gets 12px type and holds as many lines as fit (termMax), fills from the top, and loops without clearing so it stays full. THE WORK head has ◀ 06 / 17 ▶ arrows (delegated click on .ft-arrow) and a blank OPEN slot when there is no href so the row never shifts. Measured: every slot sums to 636 at 1400.
- **Hero title slider follows the rack:** every P entry has a `title:`; mount() dispatches `patch:project`, the slider in ai.html slews to that notch. Jade may swap pairings (table in the 2026-09-07 chat).
- **Jade, 2026-09-07: "you keep bringing things over with the wrong elements and UI"** (the MANGLER was redrawn house-style in another window; today's safetensors byte-map, patch rack and tier board are drawings of the data, not a product UI). Then: "i dont want screenshots, you have all the original project files, why not bring the code exactly over where it makes sense?" LAW: the element is the project's own code brought over as it is (its markup, CSS, JS, or for a CLI tool its source file and its real run), never a drawing of its data. DONE the same day: the three CLI pages now show the real source on the left (check.py:19-52, validate.py:1-81, screen.py:24-74, read from ~/demos at build time with line numbers) and on the right the same function ported line for line running on the real fixtures / test cases / case files, printing exactly what the Python prints (check.py:89-97 line formats, test_validate.py:29-32, xxd layout).
- **Height option tried and rejected**: `.ft-mount{height:640px;overflow:auto}` on slots 01, 05, 08 cuts the words mid-paragraph with no visible scroll cue (macOS overlay scrollbars). Content height stays; the page below still moves on a project change. Jade's call if she wants it fixed anyway: give .ft-mount a height and inner scroll, and add a visible fade or "more" cue.

## LOCKED (Jade, 2026-09-07 evening): "first 2 sections are great, lets lock that in dont edit that"
The hero (title, slider, contact row) and THE WORK (rack, bank, the inline stage, terminal, arrows) are locked. Do not edit them. Work is now everything BELOW AI Work.

## BELOW AI WORK, done 2026-09-07 evening (the /loop pass)
- Every card blurb below AI Work (Systems 3, Plugins 3 + subtitle, Web Apps 4, Websites 6 + subtitle, iOS 2 + subtitle, 8 video cards, SYNTH PATCHES / Sound Design / Music subtitles, Brands subtitle) rewritten in plain first person from real file:line facts gathered by an Explore agent (its report is in this session; the numbers: 90 TOI instruments / 35,553 Cmajor lines, 21 Sound Capsule FX, 8 Ambient Audio FX, 112 store products, 416 modules / 184 commits, 1,424 jamworld commits, 17 API functions, 8,875 / 4,016 Swift lines, 69 sound files, web app line counts from wc -l).
- AI & Human Oversight: each pillar ends in a real quoted line with its file:line and jumps to the rack slot that proves it (tutor effort:"low" → 01, screen.py:65 → 05, NIGHTLY-PROMPT.md:9 + 237 pending → 02).
- Websites grid is 3 × 2. Dark theme fixed for the pillars, the media-card thumbs, the Sound Design circles and play button, the brand logos (white in dark; the rule needs `section .brands-strip img` to beat theme.css:38).
- The "Trusted by major brand names..." line was NOT supported by anything on disk (the agent found: DistroKid referral ads Jade made, zZounds affiliate id, videos covering Akai / Arturia / Sequential / Elektron / Erica / TE / Roland, nothing at all for Native Instruments, Strymon, Yamaha). It now reads "Brands whose gear runs through my videos, my patch packs and my store links."

## NEXT CLEAR TASK (do without Jade, in this order)
1. **App-breadth fold is BUILT as a proposal, waiting on Jade (do not flip it yourself):** demo-apps.html (the watch case from demo-jellyboi.html, the 16 real simulator captures by name, facts from PROJECTS-INVENTORY.md rows 10/11/28/60) and a commented-out `APPS` entry in patch.js right above JELLYBOI (its four modules are the first module of each of the four racks, verbatim). To flip: uncomment the entry, delete the JELLYBOI / NOISEFACE / WAVEFACE / TODOMAI entries, bank goes 17 to 14. If Jade says no, delete demo-apps.html and the comment.
2. **Live run when ANTHROPIC_API_KEY lands:** `cd ~/demos/ai-use-case-screener && uv venv .venv && uv pip install --python .venv/bin/python anthropic pydantic && .venv/bin/python eval.py` fills runs/; then the screener element can show the real memo (render() at screen.py:91-108) for the selected case, and the terminal shows `eval.py --saved`. Same for the patch assistant (`assistant.py -v "..." --save patches/drone.json`).
3. Hero title list trim and the contact row (LinkedIn, résumé) are holds; see below.

## What could be better (honest list, 2026-09-07, end of day)
- Five app racks (JELLYBOI, NOISEFACE, WAVEFACE, TODOMAI, KEYBOARD) still dilute the AI story; task 1 above is the proposal.
- The patch assistant rack element is mostly empty module faces at 1400 (real jacks and cables, but a lot of white); a knob or two from the STERN set on the osc modules would fill it without inventing anything, if the catalog entries carried params (they do not).
- No live model run anywhere until ANTHROPIC_API_KEY lands; the screener and patch assistant show the rubric, the cases and the validator, not a model answer.
- The section is content height, so the page below moves on a project change (choice noted above).

## Copy-paste for the next window
> Read ~/Downloads/jadewii-combined/HANDOFF-AI.md and PROJECTS-INVENTORY.md first. Serve on :8765 (python3 /tmp/nocache_server.py) and open ai.html. Do the NEXT CLEAR TASK in order: 1 is built and waits on Jade (say so, do not flip it), so start at 2 if ANTHROPIC_API_KEY is in the environment, otherwise work the "What could be better" list top down. Follow all 7 Laws: no made-up content (real file:line, real command output), viz must mean something, NO em dashes, no self-explaining lines, no window and no iframe. QA over CDP in real time at 1400 and 390, light and dark (scratchpad cdp-ft.mjs, or rebuild it from the description in this file). Do not touch index.html/site.js/artwork.html and never stage them. Commit only the files you changed and push to origin main. When done say "done" with what to look at and move to the next item without asking. Work nonstop.

## Holds (need Jade)
- Brands strip: Native Instruments, Strymon and Yamaha have no video, ad or affiliate file on disk. Keep them (if there is history off-disk) or drop the three logos.
- todomai.us: the site source (~/Developer/Projects/todomai-site) never mentions the domain; confirm it resolves, or point the card at the App Store listing. Tokei Garden is a prototype (README "not a finished production app"), the card says so now; keep or cut.
- Structure: SYNTH PATCHES, Sound Design and Artwork sit in front of the "Educational videos & music" button while Law 1 says tangential creative work goes behind it. Moving them is a one-line change to the toggle's selector list plus the classes; not done without a yes.

- The job posting she is applying to (paste it): match the page's vocabulary and order to it.
- ANTHROPIC_API_KEY so the patch assistant / screener terminals can show a live run.
- LinkedIn URL and a résumé PDF for the contact row; confirm the email shown (dawlessjammin@gmail.com).
- Cut the hero title list; swap ai.html into index.html when ready; delete unskoolerz/.
